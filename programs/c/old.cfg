MEMORY {
	SCREEN:		start = $0200, size = $05C0, type = rw;
	MISC:		start = $05C1, size = $000B, type = rw;
	MAIN:		file = %O, define = yes, start = $05CC, size = $0800;
	DATA:		start = $0DCC, size = $0234, type = rw;
}
SEGMENTS {
	CODE:		load = MAIN, type = ro;
	LOWCODE:  load = DATA, run = DATA,     type = ro,  optional = yes;
	STARTUP:  load = DATA, run = DATA,     type = ro;
	ONCE:     load = DATA, run = DATA,     type = ro,  optional = yes;
	INIT:     load = DATA, run = DATA,     type = bss;
}
FEATURES {
	STARTADDRESS: default = $05CC;
	CONDES: type    = constructor,
			label   = __CONSTRUCTOR_TABLE__,
			count   = __CONSTRUCTOR_COUNT__,
			segment = ONCE;
	CONDES: type    = destructor,
			label   = __DESTRUCTOR_TABLE__,
			count   = __DESTRUCTOR_COUNT__,
			segment = RODATA;
}
